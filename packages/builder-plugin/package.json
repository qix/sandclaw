{
  "name": "@sandclaw/builder-plugin",
  "version": "0.0.1",
  "description": "Sandclaw Builder plugin — Docker-based code builds via Confidante with human approval",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build-docker": "docker build -t builder-plugin docker/",
    "run": "tsx scripts/run-build.ts"
  },
  "dependencies": {
    "@sandclaw/gatekeeper-plugin-api": "^0.0.1",
    "@sandclaw/muteworker-plugin-api": "^0.0.1",
    "@sandclaw/confidante-plugin-api": "^0.0.1",
    "@sandclaw/confidante-util": "^0.0.1",
    "@sandclaw/ui": "^0.0.1",
    "cac": "^7.0.0"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "*",
    "typescript": "*"
  }
}
